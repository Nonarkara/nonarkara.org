.DS_Store
.wrangler/
# Voice samples are biometric. Not in the repo, not in the deploy.
public/

.claude/worktrees/
dist/
worker/.dev.vars

# Written by ship.sh from NON_VERSION — never committed, so it cannot drift.
/version.json

# env files
.env
.env.*
!.env.example
